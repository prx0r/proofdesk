"""Confidence benchmark suite for document signing decisions.

Measures: when should the system sign vs defer vs reject?
Uses: Nutrient-style multi-signal confidence, conformal risk control,
      risk-coverage curves, BAS metric, calibration error.
"""
